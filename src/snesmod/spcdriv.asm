; modified for 64tass:
; snesmod.asm = snesmod_dev.asm from the snesmod distribution
; snesmod.inc = snesmod.inc from the distribution

; sm_spc.bin is the same as the distributed one.

SNESMOD_SPC .binary "sm_spc.bin"
SNESMOD_SPC_end